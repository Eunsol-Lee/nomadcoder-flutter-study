import 'package:flutter/material.dart';

class NumberOneLowerPage extends StatelessWidget {
  const NumberOneLowerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.brown.shade600,
      body: const Center(
        child: Text('Number One Lower Page'),
      ),
    );
  }
}
